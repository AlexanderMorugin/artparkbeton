export interface ICatalog {
  id: number;
  title: string;
  subtitle: string;
  route: string;
}

export interface ICatalogListCardImage {
  id: number;
  route: string;
  title: string;
  subtitle: string;
  image: string;
}
